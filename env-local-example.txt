# Copy to .env.local and adjust as needed.

# Django REST API base URL (Phase 1 backend).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Your frontend's own public URL — used for SEO (canonical links,
# sitemap.xml, Open Graph tags). Update to your real domain once live
# (e.g. https://techlora.in).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
